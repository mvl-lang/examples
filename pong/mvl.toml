[package]
name = "pong"
version = "0.1.0"
license = "Apache-2.0"
requires-mvl = ">=0.246.1"

[dependencies]
"github.com/mvl-lang/pkg-tui" = { git = "https://github.com/mvl-lang/pkg-tui", tag = "v1.2.1", rationale = "Raw-mode terminal control, keypress input, ANSI styles — POSIX stty + escapes (no extra crates); same version snake_game uses" }
