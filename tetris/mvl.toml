[package]
name = "tetris"
version = "0.1.6"
license = "Apache-2.0"
requires-mvl = ">=1.4.0"

[dependencies]
"github.com/mvl-lang/pkg-tui" = { git = "https://github.com/mvl-lang/pkg-tui", tag = "v1.2.1", rationale = "Raw-mode terminal control, keypress input, ANSI styles — POSIX stty + escapes (no extra crates); same version pong/snake_game use" }
