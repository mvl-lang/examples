[package]
name = "snake_game"
version = "0.2.2"
license = "Apache-2.0"
requires-mvl = ">=0.184.0"

[dependencies]
"github.com/mvl-lang/pkg-tui" = { git = "https://github.com/mvl-lang/pkg-tui", tag = "v1.2.1", rationale = "Raw-mode terminal control, keypress input, ANSI styles — POSIX stty + escapes (no extra crates); 1.2.1 fixes unused import warning in bridge.rs" }
