[package]
name = "sqlite_basic"
version = "0.1.0"
license = "Apache-2.0"
requires-mvl = ">=0.205.0"

[dependencies]
"github.com/mvl-lang/pkg-sqlite" = { git = "https://github.com/mvl-lang/pkg-sqlite", tag = "v0.1.4", rationale = "SQLite FFI bindings via rusqlite — native code, not generatable" }
